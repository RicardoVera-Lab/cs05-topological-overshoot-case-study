# Frozen Core Note

`src/cs05_exp001_pipeline.py` is copied byte-for-byte from the pre-real-data frozen analysis capsule.

Its SHA-256 is:

`1bbd0f67adb3c5f5f71160b4318e6a85411c9056921e3193929ea1e092a3bf0a`

The public `tests/validate_synthetic.py` is a repository-layout adapter of the original synthetic harness so that the same adversarial scenarios can be executed against `src/` in CI.

The original pre-real-data test-harness hash is preserved in `REAL_DATA_FREEZE_SHA256.json`.
