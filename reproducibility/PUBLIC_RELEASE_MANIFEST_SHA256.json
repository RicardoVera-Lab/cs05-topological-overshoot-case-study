{
  "note": "Intentionally omitted from the live GitHub tree. The immutable master archive and frozen-core hashes are preserved separately; repository files should be verified against the tagged release when created."
}